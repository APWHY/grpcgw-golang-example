apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-ingest.fullname" . }}-test-connection"
  labels:
{{ include "data-ingest.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "data-ingest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
